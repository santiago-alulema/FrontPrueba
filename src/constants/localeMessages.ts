import localEn from 'lang/en.json';
import localEs from 'lang/es.json';
import { LocaleMessagesType } from 'types/lang';

export const localeMessages: LocaleMessagesType = {
  en: localEn,
  es: localEs,
};
